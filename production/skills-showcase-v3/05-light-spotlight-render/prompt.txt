生成一个 1920x1080、30fps、6 秒的聚光灯扫字动画。深色背景为 #07090D，中心文字为“vibe motion”，使用 JetBrains Mono 粗体。聚光区域使用 210x164 的椭圆光罩，一次完整照亮两个字母；摆动范围为 -42° 到 +42°，2 秒往返循环。基础文字色为 #454545，高亮色为 #F5F5F5；辉光透明度为 0.35，模糊为 stdDeviation 2 6。画面只保留动画本身，不加入 Skill 标题页、教程说明或修改过程。所需 SVG 和矢量素材全部由 Codex 本地生成。输出为 H.264 MP4，无音轨。